const os = require('os');

console.log(
  // os.arch(), // underlying architecture
  // os.cpus(),
  // os.freemem(),
  // os.totalmem(),
  // os.homedir(),
  // os.hostname(),
  os.platform(),
  // os.release(),
  // os.tmpdir(),
  // os.uptime(),
  // os.userInfo()
);
